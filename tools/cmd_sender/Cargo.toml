[package]
name = "cmd_sender"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
prost = "0.12"
anyhow = "1.0"
crossterm = "0.27"

[build-dependencies]
prost-build = "0.12"
